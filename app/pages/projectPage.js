var React = require('react');

/**
 * ProjectPage  Project page with info.
 * @class PledgePage
 */
var ProjectPage = React.createClass({
	render() {
		return (
			<div>Place holder project page.</div>
		);
	}
});

module.exports = ProjectPage;
